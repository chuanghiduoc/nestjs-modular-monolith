generator client {
  provider     = "prisma-client"
  output       = "../src/platform/prisma/generated"
  runtime      = "nodejs"
  moduleFormat = "cjs"
}

datasource db {
  provider = "postgresql"
  schemas  = ["auth", "users", "audit", "upload", "messaging", "tenancy", "billing", "notifications"]
}
